/*console.log(window) --> in browser it opens a window object
console.log(document) --> it opens only the document 
console.dir(document) --> it gives the document as well as the other things we get from window object as well 
console.log() -->Displays the element as an HTML markup tree.
console.dir() -->Displays the element as a pure JavaScript object.
*/

/*
DOM diagram
                    window
                       |
                    document
                        |
                       html
                |        |                |
               Head      |              Body
            |    |       |                |
        title   meta     |               div -> attribute
          |       |      |          |        |
    text node   attribute|attribute<-h1      p
                         |           |         |
                         |       text node   text node
                         |       
*/

/*
some dom manipulation methods

* title.getElementById
* title.getAttribute("title")
* title.getAttribute("title").setAttribute("class", "test heading")

* title.innerText()-> gives only the showed text
* title.textContent() -> gives the hidden text content as well
* title.innerHtml()-> provides the whole html

* document.querySelector('') --> selects the first particular mentioned inside() in the mentioned page
* document.querySelectorALl('') -->selects all the elements mentioned inside() in the page
returns a nodelist(its different from array)
* document.getElementByClassName('') --> returns a html collection of the items said

we can convert it in array using Array.from(name of the html collection)

*/